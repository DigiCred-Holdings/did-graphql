{
  "name": "@digicred-holdings/did-graphql-server",
  "version": "0.4.0",
  "description": "Server-side ZCAP-LD invocation checking for a GraphQL resource server — verifies did:key + eddsa-jcs-2022 entirely in-process (no agent, no database). Includes an explicit UNSAFE_MODE for dev/test without real capabilities.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DigiCred-Holdings/did-graphql.git",
    "directory": "server"
  },
  "homepage": "https://github.com/DigiCred-Holdings/did-graphql/tree/main/server#readme",
  "bugs": {
    "url": "https://github.com/DigiCred-Holdings/did-graphql/issues"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": [
    "dist",
    "src",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "bs58": "^6.0.0",
    "canonicalize": "^2.0.0",
    "graphql": "^16.14.2"
  }
}
